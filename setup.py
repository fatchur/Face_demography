from setuptools import setup, find_packages

setup(
    name='face_demography',  # Required
    version='v0',  # Required
    description='a collection of face gender, age, and expression model',  # Required
    author='Chur Chur',  # Optional
    packages=["face_demography"],  # Required
)
